// Rumi should ignore comments

/*
And multi line comments
*/

/* And /* nested */ multi line comments*/

c_func := (a: int) -> int;

printf := (t: string, c: ...any)->int;

vardtest := (v: ...int) -> int{
  printf("Inside the %s function\n", "verdtest");
  if (1)
    printf("inside the first if\n"); 
  
  if (1){
    printf("from inside the if\n");
  } else {
    printf("this shouldn't print\n"); 
  }
  if(1)
    if(0)
      printf("doesn't matter\n");
    else
      printf("This is from the inside else");
  return 1;
}

test := (a: int, b: int)-> int{
  return a + b * (b+2) + vardtest(1,2,3,9);
}

main := () -> int{
  a := 2;
  b : int;
  while (a) {
    printf("within while\n");
    a = a - 1;
  }
  return test(2, 3);
}