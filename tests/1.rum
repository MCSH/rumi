// Rumi should ignore comments

/*
And multi line comments
*/

/* And /* nested */ multi line comments*/

c_func := (a: int) -> int;

printf := (t: string, c: ...any)->int;

vardtest := (v: ...int) -> int{
  printf("Inside the %s function\n", "verdtest");
  return 1;
}

test := (a: int, b: int)-> int{
  return a + b * (b+2) + vardtest(1,2,3,9);
}

main := () -> int{
  return test(2, 3);
}