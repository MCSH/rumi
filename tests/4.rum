import base
import compiler

@define
add_id := (c: *compiler, s: *statement) -> int {

  s.add_field("id", "int");
  printf("From inside!\n");

  return 0;
}

@add_id
MyStruct : struct {
  a: int = 0;
}

main := () -> int{

  m: MyStruct;

  m.id = 2;

  printf("m.id: ", m.id);

  return 0;
}
