import base

List : struct{
  id: int;
  next: *List;
}

main := () -> void {
  l : *List;
  l2 : *List;
  l = malloc(sizeof(List))->*List;
  l2 = malloc(sizeof(List));

  l2.id = 20;

  l.next = l2;

  printf("l2.id = %d\n",l.next.id);
}