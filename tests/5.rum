

printf := (t: string, f: ...any) -> int;

main := ()->int{
  arr : int[10];
  x := 3;

  arr[2] = 1;

  a := 2;

  arr[a] = 4;

  printf("the array's 3rd member was %d\n", arr[a]);

  return 0;

  // arr2 : int[x];
  // arr : []int;
}