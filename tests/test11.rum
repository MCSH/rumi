import test_import

Vector2d: struct{
  x: int;
  y: int;
}

new Vector2d := () -> unit{
  self.x = 10;
  self.y = 20;
  printf("inside the new function\n");
  return ;
}
main := ()->unit{
  pos : Vector2d;
  printf("pos=<%d, %d>\n", pos.x, pos.y);
  return;
}