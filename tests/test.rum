printInt := (i: int) -> unit;
printString := (s: string) -> unit;

// testing empty semicolons
;
;
main := () -> int {
  i := 2;
  a : s64 = 2;

// testing empty semicolons
;
  while(i){
    i = i - 1;
    a = a * a;
    printInt(a);
  }

  if(a)
    a = a - 10;
  else
    a = a + 4;

  c: u16;
  c = a -> u8;

  printString("Hello Rumi!\n\"\\ We're inside the string!!!");

  return c;
}
// Comment at the end (#2)
