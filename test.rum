//a :() -> int;
//f: (int, int)->*int;

//g: (*(int)->int, int) -> unit;

a := 1;
t := f(0, f());
tval := 1;
main := (i: int, j: f32, a: *string) -> unit{
  a := 1;
  a = 1;
  // line
  // after liune
  a := //
  2;

  a := 1 + 2;

  //fval := f(11, 12, 1 + 2 * 2, f());
  
  fval := f(f());

  return;
  while(a) if(a) return 1;
  if(a){
  }
  if(a) return 0;
  else return b;
  return a;
}
empty := (){}

test_op := (){
  a := 1 + 2 * 3 - 4 / 6 % 5;
}