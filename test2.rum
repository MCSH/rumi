printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;
free := (i: *any) -> unit;

TestS: struct{
  id: int;
  name: string;
}

main := () -> int {

  t: TestS;

  return 0;
}