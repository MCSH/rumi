printInt := (i: int) -> unit;
printString := (s: string) -> unit;

main := () -> int {
  i := 2;
  a := 2;

  while(i){
    i = i - 1;
    a = a * a;
    printInt(a);
  }

  if(a)
    a = a - 10;
  else
    a = a + 4;

  printString("Hello Rumi!\n\"\\ We're inside the string!!!");

  return a;
}