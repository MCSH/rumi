printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;
free := (i: *any) -> unit;

TestS: struct{
  name: string;
  id: int;
}

main := () -> int {

  t: TestS;

  t.id = 2;
  t.name = "Sajjad";

  printInt(t.id);
  printString(t.name);

  return 0;
}