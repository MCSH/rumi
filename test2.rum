printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;
free := (i: *any) -> unit;
printf := (f: string, a: ...any)->unit;

TestS : struct{
  id: int;
}

TestS.print := () -> unit{
  printf("<TestS id=%d>\n", self.id);
  return ;
}

TestS.getId := () -> int{
  return self.id;
}

main := () -> int {
  t : TestS;
  t.id = 10;

  t.print();
  tmp := t.getId();

  t2 := t.id;

  printf("t.getId() = %d\nt.id=%d\n", tmp, t2);

  return 0;
}