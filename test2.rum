printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;

main := () -> int {

  i: int = 0;
  ptr : *int = malloc(sizeof int) -> *int;

  *ptr = 3;

  printInt(i);
  printInt(*ptr);

  return 0;
}