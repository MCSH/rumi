printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;
free := (i: *any) -> unit;

InnerTest: struct{
  id: int;
}

TestS: struct{
  name: string;
  id: int;
  it: InnerTest;
}

main := () -> int {

  t: TestS;

  t.id = 2;
  t.name = "Sajjad";

  t.it.id = 5;

  printInt(t.id);
  printString(t.name);
  printInt(t.it.id);

  return 0;
}