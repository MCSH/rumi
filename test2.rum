printInt := (i: int) -> unit;
printString := (s: string) -> unit;
malloc := (i: int) -> *any;
free := (i: *any) -> unit;
printf := (f: string, a: ...any)->unit;

TestS : struct{
  id: int;
}

TestS.print := () -> unit{
  printf("<TestS id=%d>", self.id);
  return ;
}

main := () -> int {
  t : TestS;
  t.id = 10;

  // t.print();

  return 0;
}