/*
        This file is never compiled, the import compiler statement is handled differently.

        This is just to show the structure generated when you import compiler
*/

compiler : struct {
}

compiler.exit := (status: int)->void{}

statement : struct{
}

statement.add_field := (field: string, t: string)->void{}